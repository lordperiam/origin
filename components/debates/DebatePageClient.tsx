"use client"
import { useEffect, useState } from "react"
import { notFound } from "next/navigation"
import DebateDetails from "@/components/debates/debate-details"

// Placeholder DebatePageClient component for build
export default function DebatePageClient({ debateId }: { debateId: string }) {
  return <div>DebatePageClient placeholder. debateId: {debateId}</div>
}
